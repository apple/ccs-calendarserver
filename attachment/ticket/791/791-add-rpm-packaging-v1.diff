Index: redhat/kerberos.spec
===================================================================
--- redhat/kerberos.spec	(revision 0)
+++ redhat/kerberos.spec	(revision 0)
@@ -0,0 +1,40 @@
+%define modname kerberos
+%define version 1.1.1
+%define unmangled_version 1.1.1
+%define release 1
+%{!?python: %define python python26}
+
+Summary: Kerberos high-level interface
+Name: %{python}-%{modname}
+Version: %{version}
+Release: %{release}
+Source0: %{modname}-%{unmangled_version}.tar.gz
+License: ASL 2.0
+Group: Development/Libraries
+BuildRoot: %{_tmppath}/%{name}-%{version}-%{release}-buildroot
+Prefix: %{_prefix}
+Vendor: UNKNOWN <UNKNOWN>
+
+%description
+
+This Python package is a high-level wrapper for Kerberos (GSSAPI)
+operations.  The goal is to avoid having to build a module that wraps the
+entire Kerberos.framework, and instead offer a limited set of functions
+that do what is needed for client/server Kerberos authentication based
+on <http://www.ietf.org/rfc/rfc4559.txt>.
+
+%prep
+%setup -n %{modname}-%{unmangled_version}
+
+%build
+env CFLAGS="$RPM_OPT_FLAGS" %{python} setup.py build
+
+%install
+%{python} setup.py install -O1 --root=$RPM_BUILD_ROOT --record=INSTALLED_FILES
+
+%clean
+rm -rf $RPM_BUILD_ROOT
+
+%files -f INSTALLED_FILES
+%defattr(-,root,root)
+%doc README.txt LICENSE
Index: redhat/makefile
===================================================================
--- redhat/makefile	(revision 0)
+++ redhat/makefile	(revision 0)
@@ -0,0 +1,19 @@
+RPMDIR := $(PWD)/redhat
+PYTHON := python26
+SUBDIRS := BUILD RPMS SOURCES SPECS SRPMS
+DIRS := $(foreach subdir,$(SUBDIRS),$(RPMDIR)/$(subdir))
+
+rpm: init
+	python setup.py sdist -d $(RPMDIR)/SOURCES
+	rpmbuild --define "_topdir $(RPMDIR)" --define "python $(PYTHON)" -ba $(RPMDIR)/kerberos.spec
+
+init: testdir
+	mkdir -p $(DIRS)
+
+clean: testdir
+	rm -rf $(DIRS)
+
+testdir:
+ifeq ($(wildcard setup.py),)
+	$(error Must be run from same directory as setup.py)
+endif
Index: README.txt
===================================================================
--- README.txt	(revision 10189)
+++ README.txt	(working copy)
@@ -34,6 +34,7 @@
       edu.mit.Kerberos : example Kerberos .ini file.
     README.txt         : this file!
     kerberos.py        : Python api documentation/stub implementation.
+    redhat/            : Package metadata for building RPMs
 
 =====
 BUILD
@@ -43,6 +44,15 @@
 
     python setup.py build
 
+To build an RPM package, run:
+
+    make -f redhat/makefile
+
+or to use a specific python version:
+
+    make PYTHON=python27 -f redhat/makefile
+
+
 =======
 TESTING
 =======
Index: MANIFEST.in
===================================================================
--- MANIFEST.in	(revision 0)
+++ MANIFEST.in	(revision 0)
@@ -0,0 +1,3 @@
+recursive-include src *.h
+include README.txt
+include LICENSE
